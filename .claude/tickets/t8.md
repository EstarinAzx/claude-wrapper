Slice 8 of spec #120. **Blocked by #121, #122, #123, #124, #125, #126, #127 — the owner said "towards the end", and this is that.**

## Do

Bump `package.json` from `0.1.0` to `1.0.0`.

## What was measured, so nobody re-derives it

The claim "nothing reads the version" was originally supported by a warrant that only proved the string exists — Pressure refuted that correctly. It was then re-established by direct measurement:

- No `app.getVersion()`, no `package.json` read, no `__APP_VERSION__`, no version env in `src/` or `electron.vite.config.ts`. The only `version` matches in `src/` are three unrelated code comments.
- `git tag` count is **0**.
- There is **no electron-builder config**, and the standing decision is `npm run dev` only, no installer (`.context/decisions.md` → `[[2026-07-22-dev-run-only]]`).

So 1.0.0 here is a **marker, not a release mechanism**. Nothing is published outward, which is why this slice does not require an owner call.

## Do not scope-creep

Do **not** add an About surface, a version pill, a `getVersion` call, a tag, a changelog generator or a release workflow. None were asked for. If a version readout is ever wanted, that is its own ticket with its own warrant.

## Before bumping — this is the actual work

Confirm #121-#127 are merged and the gate is green on `main`:

- `npm run typecheck` clean
- `npm test` green, and **read the new baseline off `main`** rather than trusting a number (it was 1122 tests / 74 files before this batch; every slice here adds tests)
- `npm run build` clean

A 1.0.0 that ships on a red gate is worse than no bump at all.

## Acceptance

- [ ] #121-#127 are closed and merged.
- [ ] `package.json` reads `"version": "1.0.0"`.
- [ ] The full gate is green on `main` and the new test baseline is recorded in `.context/`.
- [ ] Nothing else changed — `git diff` for this slice is one line plus context.
